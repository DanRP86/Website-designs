<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Global RE Center of Excellence</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- RESET & VARIABLES --- */
        :root {
            --glass-bg: rgba(255, 255, 255, 0.15);
            --glass-border: rgba(255, 255, 255, 0.2);
            --text-white: #ffffff;
            --accent: #d4af37; /* Gold accent */
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Lato', sans-serif;
            color: var(--text-white);
            overflow-x: hidden;
            background: #000;
        }

        h1, h2, h3 { font-family: 'Playfair Display', serif; }

        /* --- IMMERSIVE BACKGROUND SLIDER --- */
        .slider-container {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .slide {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background-size: cover;
            background-position: center;
            opacity: 0;
            transform: scale(1);
            transition: opacity 1.5s ease-in-out;
        }

        .slide.active {
            opacity: 1;
            animation: kenBurns 20s infinite alternate; /* The Slow Zoom Effect */
        }

        /* Dark overlay so text is readable */
        .slide::after {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(to bottom, rgba(0,0,0,0.3), rgba(0,0,0,0.7));
        }

        @keyframes kenBurns {
            0% { transform: scale(1); }
            100% { transform: scale(1.15); }
        }

        /* --- LAYOUT & GLASSMORPHISM --- */
        .content-wrapper {
            position: relative;
            z-index: 2;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 2rem;
        }

        .glass-card {
            background: var(--glass-bg);
            backdrop-filter: blur(12px); /* The Blur Magic */
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--glass-border);
            border-radius: 16px;
            padding: 3rem;
            max-width: 1100px;
            margin: 0 auto;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        /* --- HEADER --- */
        .header-content {
            text-align: center;
            margin-bottom: 3rem;
            border-bottom: 1px solid rgba(255,255,255,0.2);
            padding-bottom: 2rem;
        }

        h1 {
            font-size: 4rem;
            margin-bottom: 0.5rem;
            text-shadow: 0 4px 10px rgba(0,0,0,0.3);
        }
        
        .subtitle {
            font-size: 1.2rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            opacity: 0.9;
        }

        /* --- GRID SECTIONS --- */
        .grid-layout {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .info-block {
            background: rgba(0,0,0,0.4); /* Slightly darker for contrast */
            padding: 1.5rem;
            border-radius: 8px;
            border-left: 3px solid var(--accent);
            transition: transform 0.3s;
        }

        .info-block:hover {
            transform: translateY(-5px);
            background: rgba(0,0,0,0.6);
        }

        .info-block h3 { color: var(--accent); margin-bottom: 0.5rem; }
        .info-block p { font-size: 0.95rem; line-height: 1.6; opacity: 0.9; }

        .stat-row {
            display: flex;
            justify-content: space-between;
            margin-top: 1rem;
            font-size: 0.9rem;
            border-top: 1px solid rgba(255,255,255,0.1);
            padding-top: 10px;
        }

        /* --- BUTTON --- */
        .btn-glow {
            display: inline-block;
            margin-top: 2rem;
            padding: 1rem 3rem;
            background: var(--accent);
            color: #000;
            text-decoration: none;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-radius: 50px;
            box-shadow: 0 0 20px rgba(212, 175, 55, 0.4);
            transition: all 0.3s;
        }
        
        .btn-glow:hover {
            background: #fff;
            box-shadow: 0 0 30px rgba(255, 255, 255, 0.6);
            transform: scale(1.05);
        }

        /* --- RESPONSIVE --- */
        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            .glass-card { padding: 1.5rem; }
        }
    </style>
</head>
<body>

    <div class="slider-container">
        <div class="slide active" style="background-image: url('https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?ixlib=rb-4.0.3&auto=format&fit=crop&w=2560&q=85');"></div>
        
        <div class="slide" style="background-image: url('https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?ixlib=rb-4.0.3&auto=format&fit=crop&w=2560&q=85');"></div>
        
        <div class="slide" style="background-image: url('https://images.unsplash.com/photo-1518005020951-ecc8c121845f?ixlib=rb-4.0.3&auto=format&fit=crop&w=2560&q=85');"></div>
    </div>

    <div class="content-wrapper">
        <div class="glass-card">
            
            <header class="header-content">
                <p class="subtitle">Real Estate & Facilities Management</p>
                <h1>Center of Excellence</h1>
                <p>Transforming Spaces. Driving Sustainability. Maximizing Value.</p>
                <a href="#" class="btn-glow">Explore Portfolio</a>
            </header>

            <div class="grid-layout">
                <div class="info-block">
                    <h3><i class="fa-solid fa-building"></i> Dubai Regional HQ</h3>
                    <p>A landmark investment in the future of work. Our new regional headquarters features state-of-the-art collaborative spaces.</p>
                </div>

                <div class="info-block">
                    <h3><i class="fa-solid fa-leaf"></i> Sustainability First</h3>
                    <p>Achieving net-zero targets through smart building technology.</p>
                    <div class="stat-row">
                        <span>Energy: -20%</span>
                        <span>CO₂: -18%</span>
                    </div>
                </div>

                <div class="info-block">
                    <h3><i class="fa-solid fa-chart-line"></i> Asset Performance</h3>
                    <p>Beutibal Site sale completion marks a record net gain for the fiscal year.</p>
                    <div class="stat-row">
                        <span>Gain: $23M</span>
                        <span>Year: 2025</span>
                    </div>
                </div>
            </div>

            <div style="margin-top: 2rem; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 1rem; display: flex; justify-content: space-around; text-align: center;">
                <div>
                    <h2 style="color:var(--accent)">150+</h2>
                    <small>Global Assets</small>
                </div>
                <div>
                    <h2 style="color:var(--accent)">92%</h2>
                    <small>Occupancy Rate</small>
                </div>
                <div>
                    <h2 style="color:var(--accent)">$42M</h2>
                    <small>Savings Generated</small>
                </div>
            </div>

        </div>
    </div>

    <script>
        const slides = document.querySelectorAll('.slide');
        let currentSlide = 0;

        function nextSlide() {
            // Remove active class from current
            slides[currentSlide].classList.remove('active');
            
            // Calculate next index
            currentSlide = (currentSlide + 1) % slides.length;
            
            // Add active class to next
            slides[currentSlide].classList.add('active');
        }

        // Change slide every 6 seconds
        setInterval(nextSlide, 6000);
    </script>
</body>
</html>

