<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Global RE & FM Center of Excellence</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;500;700;800&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- CSS VARIABLES & RESET --- */
        :root {
            --primary: #0f172a;      /* Deep Navy */
            --secondary: #3b82f6;    /* Corporate Blue */
            --accent: #10b981;       /* Sustainability Green */
            --text-light: #f8fafc;
            --text-dark: #1e293b;
            --bg-light: #f1f5f9;
            --card-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Open Sans', sans-serif;
            color: var(--text-dark);
            background-color: #fff;
            overflow-x: hidden;
            line-height: 1.6;
        }

        h1, h2, h3, h4 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
        }

        a { text-decoration: none; transition: 0.3s; }
        ul { list-style: none; }

        /* --- UTILITIES --- */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .section-padding {
            padding: 5rem 0;
        }

        .btn {
            display: inline-block;
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: var(--secondary);
            color: white;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            background: #2563eb;
        }

        .text-center { text-align: center; }
        .text-accent { color: var(--accent); }

        /* --- ANIMATION CLASSES --- */
        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s ease-out;
        }
        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* --- HERO SECTION --- */
        .hero {
            height: 100vh;
            background: linear-gradient(rgba(15, 23, 42, 0.6), rgba(15, 23, 42, 0.4)), 
                        url('https://images.unsplash.com/photo-1512453979798-5ea90b797d5c?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: fixed; /* Parallax effect */
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
            position: relative;
        }

        .hero-content h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            text-shadow: 0 2px 10px rgba(0,0,0,0.3);
        }

        .hero-content p {
            font-size: 1.25rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        /* --- HIGHLIGHTS SECTION --- */
        .highlights {
            background-color: var(--bg-light);
            margin-top: -50px; /* Overlap effect */
            position: relative;
            z-index: 10;
            padding-bottom: 5rem;
        }

        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: var(--card-shadow);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
        }

        .card-img {
            height: 200px;
            background-size: cover;
            background-position: center;
        }

        .card-body {
            padding: 2rem;
        }

        .card h3 {
            font-size: 1.4rem;
            margin-bottom: 0.5rem;
            color: var(--primary);
        }

        .card-subtitle {
            font-size: 0.9rem;
            color: #64748b;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .key-figures {
            background: #f0f9ff;
            padding: 1rem;
            border-radius: 8px;
            margin-top: 1rem;
        }

        .key-figures div {
            font-size: 0.9rem;
            font-weight: 600;
            color: var(--secondary);
            margin-bottom: 4px;
        }

        /* --- SUSTAINABILITY SECTION --- */
        .sustainability {
            background: white;
        }

        .sus-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .stats-container {
            display: flex;
            gap: 2rem;
            margin: 2rem 0;
        }

        .stat-box {
            border-left: 4px solid var(--accent);
            padding-left: 1rem;
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 800;
            color: var(--accent);
        }

        .stat-label {
            font-weight: 600;
            color: var(--primary);
        }

        .sus-visual {
            position: relative;
        }

        .sus-visual img {
            width: 100%;
            border-radius: 20px;
            box-shadow: var(--card-shadow);
        }

        .initiative-list li {
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }
        .initiative-list i {
            color: var(--accent);
            margin-right: 10px;
        }

        /* --- DASHBOARD SECTION --- */
        .dashboard {
            background: var(--primary);
            color: white;
        }

        .dashboard-mockup {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 12px;
            padding: 2rem;
            margin-top: 3rem;
            position: relative;
            box-shadow: 0 20px 50px rgba(0,0,0,0.3);
        }
        
        /* Simulating a dashboard UI with CSS Grid */
        .mock-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            grid-template-rows: auto auto;
            gap: 1rem;
            height: 400px;
        }
        
        .mock-card {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 8px;
            padding: 1rem;
        }
        
        .mock-card.large { grid-column: span 3; grid-row: span 2; background: rgba(255, 255, 255, 0.1); display: flex; align-items: center; justify-content: center; flex-direction: column;}
        .mock-card.small { grid-column: span 1; }

        .mock-chart-line {
            width: 80%; height: 2px; background: var(--secondary); margin: 10px 0; position: relative;
        }
        .mock-chart-line::after {
            content: ''; position: absolute; height: 10px; width: 10px; background: white; border-radius: 50%; right: 0; top: -4px;
        }

        /* --- CONTACT SECTION --- */
        .contact {
            background: white;
        }

        .contact-wrapper {
            max-width: 600px;
            margin: 0 auto;
            text-align: center;
        }

        .contact-form {
            margin-top: 2rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        input, textarea {
            padding: 1rem;
            border: 1px solid #cbd5e1;
            border-radius: 8px;
            font-family: inherit;
            width: 100%;
        }

        input:focus, textarea:focus {
            outline: 2px solid var(--secondary);
            border-color: transparent;
        }

        /* --- FOOTER --- */
        footer {
            background: #0f172a;
            color: #94a3b8;
            padding: 2rem 0;
            text-align: center;
            font-size: 0.9rem;
            border-top: 1px solid #1e293b;
        }

        /* --- RESPONSIVE --- */
        @media (max-width: 768px) {
            .hero-content h1 { font-size: 2.5rem; }
            .sus-grid { grid-template-columns: 1fr; }
            .mock-grid { grid-template-columns: 1fr; height: auto; }
            .mock-card.large { grid-column: span 1; height: 200px; }
            .container { padding: 0 1.5rem; }
        }
    </style>
</head>
<body>

    <section class="hero">
        <div class="container hero-content">
            <h1 class="reveal">Transforming Spaces,<br>Driving Sustainability</h1>
            <p class="reveal" style="transition-delay: 0.2s">Global Real Estate & Facilities Management Center of Excellence</p>
            <a href="#highlights" class="btn btn-primary reveal" style="transition-delay: 0.4s">Explore Our Vision</a>
        </div>
    </section>

    <section id="highlights" class="highlights">
        <div class="container">
            <div class="cards-grid">
                
                <div class="card reveal">
                    <div class="card-img" style="background-image: url('https://images.unsplash.com/photo-1580674684081-7617fbf3d745?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80');"></div>
                    <div class="card-body">
                        <div class="card-subtitle">Regional HQ</div>
                        <h3>Dubai Regional HQ – New Investment</h3>
                        <p>Discover our new state-of-the-art office and XXX services designed for the future of work.</p>
                    </div>
                </div>

                <div class="card reveal" style="transition-delay: 0.2s">
                    <div class="card-img" style="background-image: url('https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80');"></div>
                    <div class="card-body">
                        <div class="card-subtitle">Transaction</div>
                        <h3>Successful Sale of Beutibal Site</h3>
                        <p>A major milestone for PMI strategy optimization.</p>
                        <div class="key-figures">
                            <div><i class="fa-solid fa-arrow-trend-up"></i> Cash-in: $25M</div>
                            <div><i class="fa-solid fa-coins"></i> Net Gain: $23M</div>
                        </div>
                    </div>
                </div>

                <div class="card reveal" style="transition-delay: 0.4s">
                    <div class="card-img" style="background-image: url('https://images.unsplash.com/photo-1556761175-5973dc0f32e7?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80');"></div>
                    <div class="card-body">
                        <div class="card-subtitle">Strategic Partnership</div>
                        <h3>Global Partnership Renewal with XXX</h3>
                        <p>Extending our collaboration for Integrated FM Services through 2027–2031.</p>
                    </div>
                </div>

            </div>
        </div>
    </section>

    <section id="sustainability" class="section-padding sustainability">
        <div class="container">
            <div class="sus-grid">
                <div class="sus-content reveal">
                    <h4 class="text-accent">ESG INITIATIVES</h4>
                    <h2>Committed to a Greener Future</h2>
                    <p>We are dedicated to reducing our carbon footprint through intelligent building management and renewable energy sourcing.</p>
                    
                    <div class="stats-container">
                        <div class="stat-box">
                            <div class="stat-number">20%</div>
                            <div class="stat-label">Energy Reduced</div>
                        </div>
                        <div class="stat-box">
                            <div class="stat-number">18%</div>
                            <div class="stat-label">CO₂ Down</div>
                        </div>
                    </div>

                    <ul class="initiative-list">
                        <li><i class="fa-solid fa-check-circle"></i> LED Lighting Retrofitting Global Program</li>
                        <li><i class="fa-solid fa-check-circle"></i> Automated Timer & Sensor Implementation</li>
                        <li><i class="fa-solid fa-check-circle"></i> Quarterly Energy Audits & Optimization</li>
                    </ul>
                </div>
                <div class="sus-visual reveal">
                    <img src="https://images.unsplash.com/photo-1497366216548-37526070297c?ixlib=rb-4.0.3&auto=format&fit=crop&w=1000&q=80" alt="Green Office">
                </div>
            </div>
        </div>
    </section>

    <section id="dashboard" class="section-padding dashboard">
        <div class="container">
            <div class="text-center reveal">
                <h2>Global Real Estate Dashboard</h2>
                <p style="opacity: 0.8;">Dynamic real-time view of 150+ assets tracking KPIs, Occupancy, and Spend.</p>
            </div>

            <div class="dashboard-mockup reveal">
                <div class="mock-grid">
                    <div class="mock-card small">
                        <h5 style="opacity:0.7">Total Assets</h5>
                        <h3>152</h3>
                    </div>
                    <div class="mock-card small">
                        <h5 style="opacity:0.7">Occupancy</h5>
                        <h3>87%</h3>
                    </div>
                    <div class="mock-card small">
                        <h5 style="opacity:0.7">Avg. Cost/SqFt</h5>
                        <h3>$42.50</h3>
                    </div>
                    <div class="mock-card small">
                        <h5 style="opacity:0.7">Requests</h5>
                        <h3>12 Pending</h3>
                    </div>
                    <div class="mock-card large">
                        <i class="fa-solid fa-chart-line fa-3x" style="color:var(--secondary); margin-bottom:1rem;"></i>
                        <h4>Global Portfolio Performance</h4>
                        <p style="font-size:0.8rem; opacity:0.6;">Live Data Visualization</p>
                        <div class="mock-chart-line"></div>
                        <div class="mock-chart-line" style="width: 60%"></div>
                    </div>
                    <div class="mock-card small" style="grid-column: span 1; grid-row: span 2; display: flex; flex-direction:column; justify-content:center; align-items:center;">
                         <i class="fa-solid fa-globe fa-2x" style="margin-bottom:10px"></i>
                         <div style="font-size:0.8rem">EMEA: 40%</div>
                         <div style="font-size:0.8rem">APAC: 35%</div>
                         <div style="font-size:0.8rem">AMER: 25%</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="contact" class="section-padding contact">
        <div class="container contact-wrapper reveal">
            <h2>Get in Touch</h2>
            <p>Connect with the Real Estate Center of Excellence team.</p>
            
            <div class="contact-form">
                <input type="text" placeholder="Your Name" required>
                <input type="email" placeholder="Your Email" required>
                <textarea rows="5" placeholder="Message"></textarea>
                <button class="btn btn-primary" style="border:none; width:100%;">Send Message</button>
            </div>
            
            <div style="margin-top: 2rem;">
                <p><i class="fa-solid fa-envelope"></i> realestate.coe@example.com</p>
            </div>
        </div>
    </section>

    <footer>
        <div class="container">
            <p>&copy; 2025 Global Real Estate & Facilities Management CoE. All Rights Reserved.</p>
        </div>
    </footer>

    <script>
        // Simple Intersection Observer for scroll animations
        const revealElements = document.querySelectorAll('.reveal');

        const revealOnScroll = new IntersectionObserver((entries, observer) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                    observer.unobserve(entry.target); // Only animate once
                }
            });
        }, {
            threshold: 0.15
        });

        revealElements.forEach(el => revealOnScroll.observe(el));
    </script>
</body>
</html>
